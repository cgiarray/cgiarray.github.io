---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import SearchToggleScript from '../components/SearchToggleScript.astro';

const meta = {
  title: "CGIArray – Web Animations & 3D Assets for Creators",
  description: "Download stunning CGI animation loops, 3D assets, and motion graphics — optimized for the web, creators, and small studios.",
  image: "/images/social-share.jpg",
  url: "https://cgiarray.com"
};
---

<Layout {...meta}>
  <Header />

  <!-- Hero Section -->
  <section class="hero">
    <h1>Welcome to CGIArray</h1>
    <p>Discover stunning CGI animation loops, 3D assets, and motion graphics — optimized for the web, creators, and small studios.</p>
  </section>

  <!-- Search Toggle Script -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const searchIcon = document.getElementById('search-icon');
      const searchInput = document.getElementById('search-input');

      if (searchIcon && searchInput) {
        searchIcon.addEventListener('click', (e) => {
          e.stopPropagation();
          searchIcon.style.display = 'none';
          searchInput.removeAttribute('hidden');
          searchInput.focus();
        });

        document.addEventListener('click', (e) => {
          if (!searchInput.contains(e.target)) {
            searchInput.setAttribute('hidden', '');
            searchIcon.style.display = 'inline-block';
          }
        });
      }
    });
  </script>
</Layout>
